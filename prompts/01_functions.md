```
### 🐍 Python 고급 함수 구현 요청: 사칙연산 계산기

당신은 고급 Python 프로그래밍 전문가입니다. 아래 주어진 조건과 코드 골격에 맞춰 완벽한 Python 코드를 작성해 주세요.

#### ✅ 조건:
1.  **함수명:** `sjh_calculate_all(sj_num1, sj_num2)`를 사용합니다. (이니셜 `sjh_` 준수)
2.  **변수명:** 함수 내부 및 인자 변수명은 `sj_*` 접두사를 사용합니다.
3.  **기능:** 두 숫자(`sj_num1`, `sj_num2`)를 입력받아 덧셈, 뺄셈, 곱셈, 나눗셈 결과를 **튜플** 형태로 모두 반환해야 합니다.
4.  **예외 처리:** 0으로 나누는 경우는 나눗셈 결과 대신 **"division_error"** 문자열을 반환해야 합니다.
5.  **출력:** 주어진 테스트 리스트를 사용하여 10개의 테스트 결과를 모두 출력해야 합니다.

#### 📄 문제 코드 골격 (수정하여 완성)

```python
# 문제 코드 골격

def calculate_all(num1, num2):
    # 여기에 사칙연산 구현
    # return (덧셈, 뺄셈, 곱셈, 나눗셈)
    pass


# 테스트 리스트 (10개)
test_a = [10, 25, 40, 12, 7, 9, 16, 100, 3, 81]
test_b = [5, 5, 8, 3, 0, 3, 2, 4, 9, 9]

# 테스트 실행
for i in range(10):
    a = test_a[i]
    b = test_b[i]
    # calculate_all 함수를 sjh_calculate_all로 대체하여 사용
    result = calculate_all(a, b) 
    print(f"{a}, {b} => {result}")
```

```
---

## 📋 3. 최종 결과 정리 Markdown 파일 (output_summary.md)

Gemini가 응답한 내용을 정리하고 설명하기 위한 Markdown 파일입니다.

```markdown
# 🌟 Python 함수 구현 결과 요약 및 설명

요청하신 **사칙연산 수행 함수** 구현 결과를 정리합니다. 모든 조건(함수명 `sjh_*`, 변수명 `sj_*`, 0 나눗셈 예외 처리)을 만족하도록 작성되었습니다.

---

## 1. 완성된 Python 코드

함수 `sjh_calculate_all` 내에서 사칙연산을 수행하며, 나눗셈의 경우 **ZeroDivisionError** 상황을 `division_error` 문자열로 처리하도록 구현했습니다.

```python
def sjh_calculate_all(sj_num1, sj_num2):
    """
    두 숫자에 대한 덧셈, 뺄셈, 곱셈, 나눗셈 결과를 튜플로 반환합니다.
    나눗셈에서 0으로 나누는 경우 'division_error'를 반환합니다.
    """
    sj_sum = sj_num1 + sj_num2
    sj_diff = sj_num1 - sj_num2
    sj_prod = sj_num1 * sj_num2
    
    # 0으로 나누는 경우 예외 처리
    if sj_num2 == 0:
        sj_div = "division_error"
    else:
        sj_div = sj_num1 / sj_num2
        
    # 결과 튜플 반환 (덧셈, 뺄셈, 곱셈, 나눗셈)
    return (sj_sum, sj_diff, sj_prod, sj_div)


# 테스트 리스트 (10개)
sj_test_a = [10, 25, 40, 12, 7, 9, 16, 100, 3, 81]
sj_test_b = [5, 5, 8, 3, 0, 3, 2, 4, 9, 9]

# 테스트 실행
print("### 2. 테스트 실행 결과")
for sj_i in range(10):
    sj_a = sj_test_a[sj_i]
    sj_b = sj_test_b[sj_i]
    
    # 함수 실행
    sj_result = sjh_calculate_all(sj_a, sj_b) 
    
    print(f"{sj_a}, {sj_b} => {sj_result}")
```
